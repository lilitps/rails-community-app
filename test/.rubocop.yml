inherit_from: ../.rubocop.yml

Metrics/ClassLength:
  Max:  150

Metrics/BlockLength:
  Enabled: no

# This prevents code like `expect(subject.include? something).to be true`
Style/NestedParenthesizedCalls:
  Enabled: no

Rails/SkipsModelValidations:
  Enabled: no
