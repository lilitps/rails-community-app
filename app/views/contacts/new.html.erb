<% provide(:title, t('page_title.contact')) %>

<%= render 'board_of_directors' %>

<div id="contact">
  <h1><%= t('community.contact', community_name: t('community.name')) %></h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    </div>
  </div>
</div>