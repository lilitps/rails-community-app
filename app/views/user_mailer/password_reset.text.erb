<%= t('click_link_to_reset_pw') %>

<%= edit_password_reset_url(@user.perishable_token,
                            email: @user.email,
                            locale: I18n.locale) %>

<%= t('link_will_expire') %>

<%= t('ignore_password_reset') %>